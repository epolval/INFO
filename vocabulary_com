dictionary[key] if key in dictionary else None  #sequrity way for key request
dictionary.get(key,'not mandatory string if key is not found') #OR we can use method .get
list(dictionary.keys()) #return list of keys of dictionary
list(dictionary.values()) #return list of values
list(dictionary.items()) #return list of items
list(d) #return all keys in dict d
len(d) #return number of items in dict d
d[key] = value #add new item key:value
d.pop(key,'not mandatory string if key was not found') #delete item key:value
d.popitem() #get items like a cortage
d.update(d1) #splits dictionaryes d and d1, if exist same keys item of d will be updated by item from d1
c =d.copy() #copy d to c
d.clear() #make d empty
enumerate(iterable, start=0) #add indexes for member of itarable
